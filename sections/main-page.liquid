<article class="main-page section-shell">
  <p class="eyebrow">{{ page.title }}</p>
  <h1>{{ page.title }}</h1>
  <div class="rte">{{ page.content }}</div>
</article>

{% schema %}
{ "name": "Main page", "settings": [] }
{% endschema %}
